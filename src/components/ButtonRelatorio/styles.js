import { StyleSheet, } from 'react-native'

const styles = StyleSheet.create({
    
    // BOTÕES - UI KITTEN
    
    relButton: {
        flex: 1,
        margin: 5
    },
    

})

export default styles